# This file defines the Kubernetes Service for the genai-service application.
apiVersion: v1
kind: Service
metadata:
  name: genai-service
  namespace: petclinic
spec:
  selector:
    app: genai-service
  ports:
    - port: 8084
      targetPort: 8084
