apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: visits-db-policy
  namespace: petclinic
spec:
  podSelector:
    matchLabels:
      app: visits-db
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: visits-service
      ports:
        - protocol: TCP
          port: 3306
